export default function App() {
  return (
    <main className="app-shell">
      <section className="intro-card" aria-labelledby="page-title">
        <p className="eyebrow">Bun + Vite + React + TypeScript</p>
        <h1 id="page-title">Modern web starter</h1>
        <p>
          This is a deliberately minimal starting point. The full product and
          implementation requirements live in the Sikula task file.
        </p>
      </section>
    </main>
  );
}
